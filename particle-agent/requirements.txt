# Core
PyYAML>=6.0
python-dotenv>=1.0

# HTTP / API
tenacity>=8.2

# LLM providers
# (Gemini and OpenRouter accessed via requests — no extra SDK required)

# Telegram bot
python-telegram-bot[all]>=21.0

# Google APIs
google-api-python-client>=2.100
google-auth>=2.22
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.2

# Browser automation
selenium>=4.15
webdriver-manager>=4.0

# Vector / context store
chromadb>=0.4

# Document parsing
pypdf>=3.17
python-docx>=1.0

# Scheduling
APScheduler>=3.10

# Voice — STT (cloud)
mistralai>=1.12.0,<2.0
requests>=2.31  # HF API (and general HTTP)

# Voice — TTS (cloud)
# (ElevenLabs uses requests — no extra SDK required)

# Voice — local fallback
# kokoro-onnx>=0.3

# Audio
sounddevice>=0.4
soundfile>=0.12
numpy>=1.24
